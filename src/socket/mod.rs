//! Code for managing socket connections to the frontend.
//!
//! Currently this contains a small HTTP API for controlling FerroFlow from the outside
//! (e.g. setting LiquidCAN parameters).

pub mod http;
